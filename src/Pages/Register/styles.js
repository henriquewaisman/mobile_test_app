import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: 'center'
    },
    userText: {
        marginTop: '50%',
        fontSize: '20px',
    },
    user: {
        borderWidth: '1px',
        borderRadius: '10%'
    },
    password: {
        borderWidth: '1px',
        borderRadius: '10%'
    },
    passwordText: {
        fontSize: '20px',
    },
    email: {
        borderWidth: '1px',
        borderRadius: '10%'
    },
    emailText: {
        fontSize: '20px',
    },
    link: {
        marginTop: '10%',
        borderWidth: '2px'
    }

})

export default styles;
