import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    content: {
        fontSize: '15px',
    },
    link: {
        marginTop: '10%',
        borderWidth: '2px',
        width: '20%',
    }
});

export default styles;
